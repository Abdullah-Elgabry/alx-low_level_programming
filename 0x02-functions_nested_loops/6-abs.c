#include "main.h"

/**
 * _abs -  this fun check for the absolute value for int
 *
 * @ch: input of fun
 *
 * Return:0 means success
*/

int _abs(int ch)
{
	if (ch  < 0)
		ch = (-1) * ch;
	return (0);
}
