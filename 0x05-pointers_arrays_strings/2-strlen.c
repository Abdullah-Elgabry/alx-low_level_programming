#include <stdio.h>
/**
 * _strlen - function that returns the length of a string.
 *
 * @l: string parameter input
 *
 * Return:len of str
*/
int _strlen(char *l)
{
	int c;

	for (c = 0; *l =! '\0'; s++)
		c++;

	return (c);
}
